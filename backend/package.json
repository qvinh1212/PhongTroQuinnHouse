{
  "name": "phongtro-quinnhouse-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for Quinn House room rental management.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "seed:legacy": "node scripts/seed-from-state.js",
    "check": "node --check src/server.js && node --check scripts/migrate.js && node --check scripts/seed-from-state.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "engines": {
    "node": ">=20"
  }
}
